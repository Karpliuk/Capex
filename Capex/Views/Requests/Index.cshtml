@model IEnumerable<Capex.Models.Request>

@{
    ViewBag.Title = "Capex";
}

<h2 style="text-align:center;">Список заявок</h2>
<h4 style="text-align:center;"> @Html.ActionLink("Создать", "Create", null, htmlAttributes: new { @style = "color:#5CB85C;" }) </h4>

@*@using (Html.BeginForm())
{
    @*<table class="table">
        <tr>
            <th>Состояние:</th>
            <th>Подразделение:</th>
            <th>Дата от</th>
            <th>Дата до</th>
        </tr>
        <tr>
            <td>@Html.DropDownList("State")</td>
            <td>@Html.DropDownList("Unit")</td>
            <td>@Html.TextBox("StartCreationDate")</td>
            <td>@Html.TextBox("EndCreationDate")</td>
        </tr>
     </table>
    <div class="filter" style="background-color:red; position:absolute; margin-left:-350px; width:320px;">
        <h4 style="text-align:center;">Фильтры</h4>
        <div class="row" style="padding-top:10px; padding-left:20px; font-style:italic;">
            Состояние:
            @Html.DropDownList("State")
        </div>
        <div class="row" style="padding-top:10px; padding-left:20px;  font-style:italic;">
            Подразделение:  
            @Html.DropDownList("Unit")
        </div>
        <div class="row" style="padding-top:10px; padding-left:20px;  font-style:italic;">
            Дата от:
            @Html.TextBox("StartCreationDate", "{0:yyyy-MM-dd}", new { @style = "width:50%", type = "date" })
        </div>
        <div class="row" style="padding-top:10px; padding-left:20px;  font-style:italic;">
            Дата до:
            @Html.TextBox("EndCreationDate", "{0:yyyy-MM-dd}", new { @style = "width:50%", type = "date" })
        </div>
        
        @*<br />
        <div class="row" style="text-align:center">
            <button name="action" value="filter">Фильтр</button>
        </div>
    </div>
}*@

@using (Html.BeginForm())
{
    <div class="filter">
        <div class="row" style="padding-top:18px; padding-left:5px; font-style:italic; padding-bottom:10px;">
            <div class="col-md-2">
                Состояние:
                @Html.DropDownList("State", null, htmlAttributes: new { @style = "width:180px; height:26px;" })               
            </div>
            <div class="col-md-2">
                Подразделение:
                @Html.DropDownList("Unit", null, htmlAttributes: new { @style = "width:180px; height:26px;" })
            </div>
            <div class="col-md-2">
                Дата от:
                @Html.TextBox("StartCreationDate", "{0:yyyy-MM-dd}", new { @style = "width:180px; height:26px;", type = "date" })
            </div>
            <div class="col-md-2">
                Дата до:
                @Html.TextBox("EndCreationDate", "{0:yyyy-MM-dd}", new { @style = "width:180px; height:26px;", type = "date" })
            </div>     
            <div class="col-md-4" style="text-align:right; padding-top:10px; margin-right:0px;">
                <button name="action" value="filter" class="btn btn-success" style="width:180px; height:35px; border-radius:10px;">Фильтр</button>

           </div>
            </div>
        
      </div>       
}


<table class="table">
    <tr>
        <th>
            @Html.ActionLink("№ Заявки", "Index", new { SortRequest = ViewBag.NameSortParm })
            @*@Html.DisplayNameFor(model => model.RequestID)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th>
            @Html.ActionLink("Дата создания", "Index", new { SortRequest = ViewBag.DateSortParm })
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.ActionLink("Стоимость", "Index", new { SortRequest = ViewBag.ValueSortParm })
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Currency)
        </th>
        @*<th>
                @Html.DisplayNameFor(model => model.LongDescription)
        </th>*@
<th>
    @Html.DisplayNameFor(model => model.State)
</th>
<th></th>
</tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RequestID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Currency)
        </td>
        @*<td>
                @Html.DisplayFor(modelItem => item.LongDescription)
            </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.State)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id = item.RequestID }) |
            @Html.ActionLink("Просмотр", "Details", new { id = item.RequestID }) |
            @*@Html.ActionLink("Delete", "Delete", new { id=item.RequestID })*@
        </td>
    </tr>
}

</table>
